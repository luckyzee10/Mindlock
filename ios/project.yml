name: MindLock
options:
  minimumXcodeGenVersion: 2.40.0

configs:
  Debug: debug
  Release: release

packages:
  PostHog:
    url: https://github.com/PostHog/posthog-ios.git
    from: 3.0.0

targets:
  MindLock:
    type: application
    platform: iOS
    deploymentTarget: "17.0"
    sources:
      - path: MindLock
    settings:
      base:
        PRODUCT_NAME: MindLock
        PRODUCT_BUNDLE_IDENTIFIER: com.lucaszambranonavia.mindlock
        SWIFT_VERSION: 5.0
        CODE_SIGN_ENTITLEMENTS: MindLock/MindLock.entitlements
        INFOPLIST_FILE: MindLock/Info.plist
        CODE_SIGN_STYLE: Automatic
        TARGETED_DEVICE_FAMILY: "1"
        POSTHOG_API_KEY: ""
        POSTHOG_HOST: "https://us.i.posthog.com"
    entitlements:
      path: MindLock/MindLock.entitlements
      properties:
        com.apple.developer.family-controls: true
        com.apple.security.application-groups:
          - group.com.YLUUT5U99U.mindlock
    info:
      path: MindLock/Info.plist
      properties:
        CFBundleShortVersionString: "1.01"
        CFBundleVersion: "31"
        MindLockPostHogAPIKey: $(POSTHOG_API_KEY)
        MindLockPostHogHost: $(POSTHOG_HOST)
        UILaunchScreen: {}
        UIRequiresFullScreen: true
        UISupportedInterfaceOrientations:
          - UIInterfaceOrientationPortrait
    dependencies:
      - target: MindLockMonitor
        embed: true
      - target: MindLockShieldUI
        embed: true
      - target: MindLockUsageReport
        embed: false
      - sdk: FamilyControls.framework
      - sdk: ManagedSettings.framework
      - sdk: DeviceActivity.framework
      - sdk: _DeviceActivity_SwiftUI.framework
      - package: PostHog
        product: PostHog
    postBuildScripts:
      - name: EmbedUsageReportExtension
        basedOnDependencyAnalysis: false
        script: |
          set -euo pipefail
          APP_BUNDLE="$TARGET_BUILD_DIR/$FULL_PRODUCT_NAME"
          EXT_DIR="$APP_BUNDLE/Extensions"
          SOURCE_APPEX="$BUILT_PRODUCTS_DIR/MindLockUsageReport.appex"
          DEST_APPEX="$EXT_DIR/MindLockUsageReport.appex"
          if [ -d "$SOURCE_APPEX" ]; then
            mkdir -p "$EXT_DIR"
            rm -rf "$DEST_APPEX"
            rsync -a "$SOURCE_APPEX/" "$DEST_APPEX/"
          else
            echo "warning: MindLockUsageReport.appex not found at $SOURCE_APPEX"
          fi

  MindLockMonitor:
    type: app-extension
    platform: iOS
    deploymentTarget: "17.0"
    sources:
      - path: MindLockMonitor
      - path: MindLock/Services/SharedSettings.swift
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.lucaszambranonavia.mindlock.monitor
        CODE_SIGN_STYLE: Automatic
        INFOPLIST_FILE: MindLockMonitor/Info.plist
    info:
      path: MindLockMonitor/Info.plist
      properties:
        CFBundleShortVersionString: "1.01"
        CFBundleVersion: "31"
        CFBundleDisplayName: MindLock Monitor
        NSExtension:
          NSExtensionPointIdentifier: com.apple.deviceactivity.monitor-extension
          NSExtensionPrincipalClass: $(PRODUCT_MODULE_NAME).MindLockActivityMonitor
    entitlements:
      path: MindLockMonitor/MindLockMonitor.entitlements
      properties:
        com.apple.developer.family-controls: true
        com.apple.security.application-groups:
          - group.com.YLUUT5U99U.mindlock
    dependencies:
      - sdk: DeviceActivity.framework
      - sdk: ManagedSettings.framework
      - sdk: FamilyControls.framework

  MindLockShieldUI:
    type: app-extension
    platform: iOS
    deploymentTarget: "17.0"
    sources:
      - path: MindLockShieldUI
      - path: MindLock/Services/SharedSettings.swift
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.lucaszambranonavia.mindlock.shieldui
        CODE_SIGN_STYLE: Automatic
        INFOPLIST_FILE: MindLockShieldUI/Info.plist
        CODE_SIGN_ENTITLEMENTS: MindLockShieldUI/MindLockShieldUI.entitlements
    entitlements:
      path: MindLockShieldUI/MindLockShieldUI.entitlements
      properties:
        com.apple.developer.family-controls: true
        com.apple.security.application-groups:
          - group.com.YLUUT5U99U.mindlock
    info:
      path: MindLockShieldUI/Info.plist
      properties:
        CFBundleShortVersionString: "1.01"
        CFBundleVersion: "31"
        CFBundleDisplayName: MindLock Shield
        NSExtension:
          NSExtensionPointIdentifier: com.apple.ManagedSettingsUI.shield-configuration-service
          NSExtensionPrincipalClass: $(PRODUCT_MODULE_NAME).MindLockShieldDataSource
    dependencies:
      - sdk: ManagedSettingsUI.framework
      - sdk: ManagedSettings.framework
      - sdk: FamilyControls.framework

  MindLockUsageReport:
    type: app-extension
    platform: iOS
    deploymentTarget: "17.0"
    sources:
      - path: MindLockUsageReport
      - path: MindLock/Services/SharedSettings.swift
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.lucaszambranonavia.mindlock.usage
        CODE_SIGN_STYLE: Automatic
        INFOPLIST_FILE: MindLockUsageReport/Info.plist
        CODE_SIGN_ENTITLEMENTS: MindLockUsageReport/MindLockUsageReport.entitlements
        APPEX_EMBEDDED_SUBPATH: Extensions
        APPEX_SUBPATH: Extensions
    info:
      path: MindLockUsageReport/Info.plist
      properties:
        CFBundleShortVersionString: "1.01"
        CFBundleVersion: "31"
        CFBundleDisplayName: MindLock Usage Report
        EXAppExtensionAttributes:
          EXExtensionPointIdentifier: com.apple.deviceactivityui.report-extension
    dependencies:
      - sdk: DeviceActivity.framework
      - sdk: _DeviceActivity_SwiftUI.framework
      - sdk: FamilyControls.framework
      - sdk: ManagedSettingsUI.framework

  MindLockTests:
    type: bundle.unit-test
    platform: iOS
    deploymentTarget: "17.0"
    sources:
      - path: MindLockTests
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.lucaszambranonavia.mindlock.tests
        CODE_SIGN_STYLE: Automatic
    dependencies:
      - target: MindLock

schemes:
  MindLock:
    build:
      targets:
        MindLock: all
        MindLockMonitor: all
        MindLockShieldUI: all
        MindLockTests: [test]
    test:
      targets:
        - MindLockTests
